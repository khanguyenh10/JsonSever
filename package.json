{
  "name": "json-server",
  "version": "1.0.0",
  "description": "- json-server:\r     - Cách cài: npm install -g json-server\r     - Chạy server: json-server --watch db.json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
     "dev:server": "json-server --watch ./db/db.json --port 3000"
  },
  "author": "",
  "license": "ISC"
}
